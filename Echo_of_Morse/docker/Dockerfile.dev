FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Copy the project
COPY . .

# No build step - Next.js runs in dev mode with hot reload
CMD ["sh", "-c", "npx prisma migrate deploy && npm run dev"]
